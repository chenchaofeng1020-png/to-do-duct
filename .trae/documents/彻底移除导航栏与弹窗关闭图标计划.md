我明白了。之前通过 `.toolbar(.hidden, for: .navigationBar)` 尝试隐藏导航栏，但在部分情况下（特别是外层 `NavigationView` 存在时）可能没有生效。

以下是修正后的计划：

### 1. 彻底移除顶部导航栏
*   **修改文件**: `To-Do Duck/MainView.swift`
*   **操作**: 
    *   移除外层的 `NavigationView` 包裹（因为 `TodoHomeView` 本身是一个独立的滚动页面，且我们自定义了头部，不需要系统导航栏）。
    *   如果 `TodoHomeView` 需要导航功能（例如跳转到设置页），我们将确保在需要的地方使用 `Sheet` 或全屏覆盖，而不是依赖顶部的 `NavigationBar`。
    *   如果在 `SettingsView` 等子视图中需要导航栏，可以在子视图内部包裹 `NavigationView`。

### 2. 移除待办事项弹窗的关闭图标
*   **修改文件**: `To-Do Duck/MainView.swift`
*   **操作**: 
    *   定位到 `TodoActionSheet` 结构体。
    *   删除其中的 `xmark.circle.fill` 关闭按钮代码。用户可以通过点击背景或选择操作来关闭弹窗，或者我们可以保留下拉手柄（Capsule）作为提示。

### 3. 验证
*   确保 `TodoHomeView` 顶部没有系统自带的导航栏，只有我们自定义的包含鸭子图标的 `HeaderBar`。
*   确保点击待办事项弹出的菜单中不再有关闭图标。

确认后，我将立即执行修改。