好的，既然确认容器 ID 是 `iCloud.sdy.tododuck`，我来为你制定一个详细的排查计划。
        
            
toolName: todo_write
            
status: success
          
Todos updated: 8 items
          
## 📋 **CloudKit 同步问题排查计划**

### **🔴 高优先级步骤（必须完成）**

---

#### **步骤 1：检查 Apple Developer 后台 iCloud 容器配置**

**操作步骤：**

1. 访问 [Apple Developer Account](https://developer.apple.com/account/) 并登录

2. 进入 **Certificates, Identifiers & Profiles** → **Identifiers**

3. 点击 **iCloud Containers** 标签页
   - ✅ 确认是否存在容器：`iCloud.sdy.tododuck`
   - ✅ 如果不存在，点击 `+` 创建：
     - Description: `To-Do Duck Sync`
     - Identifier: `iCloud.sdy.tododuck`

4. 进入 **App IDs** 标签页
   - 找到你的 App ID（通常是 `sdy.To-Do-Duck`）
   - 点击编辑
   - ✅ 确认 **iCloud** 功能已启用
   - ✅ 点击 iCloud 右侧的 **Edit** 按钮
   - ✅ 确认 **CloudKit** 已勾选
   - ✅ 确认容器列表中已选中 `iCloud.sdy.tododuck`
   - ✅ 点击 **Save** 保存

**预期结果：**
- iCloud 容器 `iCloud.sdy.tododuck` 存在
- App ID 已启用 CloudKit
- 容器已关联到 App ID

**如果失败：**
- 记录错误信息
- 截图保存配置页面

---

#### **步骤 2：验证 Xcode 项目 Signing & Capabilities 配置**

**操作步骤：**

1. 在 Xcode 中打开项目
2. 点击左侧项目导航器最顶部的 **To-Do Duck**（蓝色图标）
3. 选择 **TARGETS** → **To-Do Duck**（主 App）
4. 点击 **Signing & Capabilities** 选项卡

**检查项：**

✅ **Signing** 部分：
- Team 已选择（你的开发者账号）
- Bundle Identifier: `sdy.To-Do-Duck`

✅ **iCloud** 部分（如果没有，点击 `+ Capability` 添加）：
- ☑️ **CloudKit** 已勾选
- Containers 列表中包含 `iCloud.sdy.tododuck` 且已勾选
- 如果显示红色错误，点击刷新按钮

✅ **Background Modes** 部分（如果没有，点击 `+ Capability` 添加）：
- ☑️ **Remote notifications** 已勾选

**预期结果：**
- 所有配置项都正确勾选
- 没有红色错误或警告

**如果失败：**
- 截图保存 Signing & Capabilities 页面
- 记录任何错误信息

---

#### **步骤 3：检查 entitlements 文件配置一致性**

**操作步骤：**

1. 打开 [To-Do Duck.entitlements](file:///Users/mycode/To-Do%20Duck/To-Do%20Duck/To-Do%20Duck.entitlements)
2. 打开 [TodoWidgetExtension.entitlements](file:///Users/mycode/To-Do%20Duck/TodoWidgetExtension.entitlements)

**检查主 App entitlements 文件：**

✅ 必须包含以下内容：
```xml
<key>com.apple.developer.icloud-container-identifiers</key>
<array>
    <string>iCloud.sdy.tododuck</string>
</array>
<key>com.apple.developer.icloud-services</key>
<array>
    <string>CloudKit</string>
</array>
<key>com.apple.security.application-groups</key>
<array>
    <string>group.sdy.tododuck</string>
</array>
<key>com.apple.developer.aps-environment</key>
<string>development</string>
```

**检查 Widget Extension entitlements 文件：**

✅ 必须包含以下内容：
```xml
<key>com.apple.developer.icloud-container-identifiers</key>
<array>
    <string>iCloud.sdy.tododuck</string>
</array>
<key>com.apple.developer.icloud-services</key>
<array>
    <string>CloudKit</string>
</array>
<key>com.apple.security.application-groups</key>
<array>
    <string>group.sdy.tododuck</string>
</array>
```

**预期结果：**
- 两个文件的容器 ID 完全一致：`iCloud.sdy.tododuck`
- App Group ID 完全一致：`group.sdy.tododuck`
- 主 App 有 `aps-environment`，Widget 可以没有

**如果失败：**
- 记录不一致的地方
- 修改文件使其一致

---

#### **步骤 4：验证 iCloud 账号登录状态和权限**

**操作步骤：**

**在 Mac 上：**
1. 打开 **系统设置** → **Apple ID**
2. ✅ 确认已登录 iCloud 账号
3. 点击 **iCloud**
4. ✅ 确认 **iCloud Drive** 已开启
5. 点击 **iCloud Drive** 右侧的 **i** 按钮
6. ✅ 确认 **"使用 iCloud 的 App"** 列表中包含 `To-Do Duck`（开发阶段可能不显示）

**在 iPhone/iPad 上（如果有）：**
1. 打开 **设置** → **你的名字**
2. ✅ 确认已登录 iCloud 账号（与 Mac 相同）
3. 点击 **iCloud**
4. ✅ 确认 **iCloud Drive** 已开启
5. 点击 **iCloud Drive**
6. ✅ 确认 **"使用 iCloud 的 App"** 列表中包含 `To-Do Duck`

**预期结果：**
- 设备已登录 iCloud
- iCloud Drive 已开启
- App 有权限访问 iCloud

**如果失败：**
- 登录 iCloud 账号
- 开启 iCloud Drive
- 重启设备

---

#### **步骤 5：运行 App 并收集控制台日志**

**操作步骤：**

1. 在 Xcode 中选择 **My Mac** 作为运行目标
2. 点击 **运行** 按钮（⌘R）
3. 等待 App 启动完成
4. 打开 **Profile** 页面
5. **开启** 云同步开关
6. **重启** App（重要！）
7. 打开 Xcode 控制台（⇧⌘C）

**需要收集的日志信息：**

✅ 启动日志：
```
🚀 Starting To-Do Duck App...
☁️ Cloud Sync Preference: Enabled/Disabled
📦 Schema created
1️⃣ Attempting App Group Storage...
🔍 App Group URL Found: ...
✅ Successfully initialized with App Group + CloudKit Mode: ...
```

✅ 错误日志（如果有）：
```
❌ SwiftData Initialization Failure: ...
❌ Detailed NSError: ...
❌ UserInfo: ...
```

✅ CloudKit 日志（如果有）：
```
CloudKit: ...
CoreData+CloudKit: ...
```

**预期结果：**
- 启动日志显示 `Cloud Sync Mode: Enabled`
- 没有红色的错误日志
- 如果有错误，记录完整的错误信息

**如果失败：**
- 复制完整的控制台日志
- 特别关注包含 `Error`、`Failed`、`❌` 的行
- 保存日志到文件

---

### **🟡 中优先级步骤（如果高优先级步骤都通过）**

---

#### **步骤 6：使用 MacProfileView 的诊断工具测试**

**操作步骤：**

1. 在 Mac 上运行 App
2. 打开 **Profile** 页面
3. 确保云同步开关已开启
4. 依次点击以下按钮并记录结果：

**测试 1：Check Cloud Data**
- 点击 **Check Cloud Data** 按钮
- ✅ 预期结果：`✅ CloudKit Available & Account Ready`
- ❌ 如果失败：记录错误信息

**测试 2：Test App Group Access**
- 点击 **Test App Group Access** 按钮
- ✅ 预期结果：`✅ App Group Write/Read Success!`
- ❌ 如果失败：记录错误信息

**测试 3：Initialize Cloud Schema**
- 点击 **Initialize Cloud Schema** 按钮
- ✅ 预期结果：`✅ Schema initialization is handled by SwiftData automatically.`

**测试 4：查看 Advanced Debug Info**
- 展开 **Advanced Debug Info**
- 记录以下信息：
  - Store Path
  - Cloud Enabled (App Init)
  - Environment

**预期结果：**
- 所有测试都显示 ✅
- Store Path 指向 App Group 目录
- Cloud Enabled 显示 YES

**如果失败：**
- 记录每个测试的结果
- 截图保存诊断信息

---

#### **步骤 7：检查数据模型关系配置**

**操作步骤：**

1. 打开 [DailyCard.swift](file:///Users/mycode/To-Do%20Duck/To-Do%20Duck/Models/DailyCard.swift)
2. 打开 [TodoItem.swift](file:///Users/mycode/To-Do%20Duck/To-Do%20Duck/Models/TodoItem.swift)
3. 打开 [MemoCard.swift](file:///Users/mycode/To-Do%20Duck/To-Do%20Duck/Models/MemoCard.swift)

**检查项：**

✅ **DailyCardV3**：
- `@Relationship(deleteRule: .cascade) var items: [TodoItemV3] = []`
- 所有属性都有默认值

✅ **TodoItemV3**：
- `var card: DailyCardV3? = nil`（Optional）
- 所有属性都有默认值
- `priorityRawValue` 有默认值 `0`

✅ **MemoCardV3**：
- 所有属性都有默认值
- `id`, `content`, `createdAt` 在 init 中初始化

**潜在问题：**
- TodoItemV3 的 `card` 关系没有显式指定 `inverse`
- 这可能导致 CloudKit 同步时的关系冲突

**建议：**
考虑为关系添加显式的 inverse，例如：
```swift
@Relationship(inverse: \TodoItemV3.card) var items: [TodoItemV3] = []
```

---

#### **步骤 8：使用真机进行同步测试**

**操作步骤：**

**准备工作：**
1. 准备两台设备（如 iPhone 和 Mac）
2. 确保两台设备登录**相同的 Apple ID**
3. 确保两台设备都有网络连接

**测试步骤：**

1. **在 Mac 上：**
   - 运行 App
   - 开启云同步
   - 重启 App
   - 创建一个测试任务："Mac 端测试"

2. **在 iPhone 上：**
   - 连接 iPhone 到 Mac
   - 在 Xcode 中选择 iPhone 作为运行目标
   - 运行 App
   - 开启云同步
   - 重启 App
   - 等待 30 秒 - 2 分钟

3. **验证同步：**
   - ✅ iPhone 上应该出现 "Mac 端测试" 任务
   - ✅ 在 iPhone 上创建新任务："iPhone 端测试"
   - ✅ Mac 上应该出现 "iPhone 端测试" 任务

**预期结果：**
- 两台设备的数据能够互相同步
- 同步延迟在 30 秒 - 2 分钟内

**如果失败：**
- 检查两台设备的 iCloud 账号是否相同
- 检查两台设备的网络连接
- 检查两台设备的云同步开关是否都已开启
- 查看控制台日志是否有错误

---

### **📊 排查结果记录表**

| 步骤 | 状态 | 结果 | 备注 |
|------|------|------|------|
| 步骤 1：Developer 后台配置 | ⬜ 待完成 | | |
| 步骤 2：Xcode 配置 | ⬜ 待完成 | | |
| 步骤 3：entitlements 文件 | ⬜ 待完成 | | |
| 步骤 4：iCloud 账号 | ⬜ 待完成 | | |
| 步骤 5：控制台日志 | ⬜ 待完成 | | |
| 步骤 6：诊断工具 | ⬜ 待完成 | | |
| 步骤 7：数据模型 | ⬜ 待完成 | | |
| 步骤 8：真机测试 | ⬜ 待完成 | | |

---

### **🚨 如果所有步骤都通过但仍然无法同步**

可能的原因：
1. **云端数据为空**：之前从未成功同步过，云端没有数据
2. **开发环境限制**：Development 和 Production 容器隔离
3. **网络延迟**：CloudKit 同步可能需要更长时间
4. **缓存问题**：需要清除本地缓存重新同步

**解决方案：**
- 使用 MacProfileView 的 **Reset Local Data** 按钮
- 清除本地数据后重新同步
- 等待更长时间观察同步情况

---

请按照这个计划逐步排查，每完成一个步骤就告诉我结果，我会根据你的反馈进一步分析问题！